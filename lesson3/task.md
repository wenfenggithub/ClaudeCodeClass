# 图书投票应用 MVP 开发任务

本文档基于 `spec.md` 拆解开发任务，用于指导 MVP 版本实现。

## 1. 项目初始化

- [x] 初始化 Node.js 项目
- [x] 创建 `package.json`
- [x] 确定后端技术方案：Node.js + Express
- [x] 配置启动命令，例如 `npm start`
- [x] 创建基础目录结构
- [x] 准备前端静态资源目录

建议目录结构：

```text
.
├── data/
│   ├── book.json
│   └── users.json
├── public/
│   ├── index.html
│   ├── style.css
│   └── app.js
├── server.js
├── package.json
├── spec.md
└── task.md
```

## 2. 数据文件任务

- [x] 创建 `data` 目录
- [x] 创建 `data/book.json`
- [x] 在 `book.json` 中准备约 5 本图书的初始数据
- [x] 每本书包含 `id`、`title`、`author`、`description`、`votes`
- [x] 确保 `votes` 初始值为数字类型
- [x] 确保 `book.json` 是合法 JSON

验收标准：

- [x] 可以正常读取 `data/book.json`
- [x] 文件中至少包含 5 本图书
- [x] 每本图书都有唯一 `id`

## 3. 后端服务任务

- [x] 创建 `server.js`
- [x] 引入 Express
- [x] 配置 JSON 请求解析
- [x] 配置静态资源服务，指向 `public` 目录
- [x] 配置服务监听端口，例如 `3000`
- [x] 启动服务后可以访问首页

验收标准：

- [x] 执行启动命令后服务可以正常运行
- [x] 浏览器访问根路径可以看到前端页面

## 4. 后端数据读写任务

- [x] 实现读取 `data/book.json` 的逻辑
- [x] 实现写入 `data/book.json` 的逻辑
- [x] 读取失败时返回清晰错误
- [x] JSON 解析失败时返回清晰错误
- [x] 写入失败时返回清晰错误

验收标准：

- [x] 后端可以读取当前图书列表
- [x] 后端可以在投票后保存最新票数
- [x] 重启服务后票数仍然保留

## 5. API 接口任务

### 获取图书列表接口

- [x] 实现 `GET /api/books`
- [x] 从 `data/book.json` 读取图书数据
- [x] 返回统一 JSON 响应格式
- [x] 成功时返回 `success: true`
- [x] 失败时返回 `success: false` 和错误信息

验收标准：

- [x] 请求 `GET /api/books` 可以返回所有图书
- [x] 返回数据包含每本书的当前票数

### 提交投票接口

- [x] 实现 `POST /api/books/:id/vote`
- [x] 从请求路径中获取图书 ID
- [x] 检查图书 ID 是否存在
- [x] 找到图书后将 `votes` 加 1
- [x] 将更新后的数据写回 `data/book.json`
- [x] 返回被投票图书的最新票数
- [x] 图书不存在时返回 404

验收标准：

- [x] 请求有效图书 ID 时，对应图书票数加 1
- [x] 请求不存在的图书 ID 时，返回错误
- [x] 投票成功后 `data/book.json` 中的数据被更新

## 6. 前端页面任务

- [x] 创建 `public/index.html`
- [x] 创建页面基础结构
- [x] 页面中包含图书列表容器
- [x] 页面中包含错误提示区域
- [x] 引入 `public/style.css`
- [x] 引入 `public/app.js`

每本图书需要展示：

- [x] 图书名称
- [x] 作者
- [x] 简短描述
- [x] 当前票数
- [x] 投票按钮

验收标准：

- [x] 打开页面后可以看到完整图书投票界面
- [x] 每本书的信息展示清楚
- [x] 每本书都有独立的投票按钮

## 7. 前端交互任务

- [x] 页面加载时请求 `GET /api/books`
- [x] 将接口返回的图书数据渲染到页面
- [x] 点击投票按钮时请求 `POST /api/books/:id/vote`
- [x] 投票成功后更新对应图书的票数
- [x] 投票请求进行中时避免重复点击
- [x] 投票失败时展示错误提示
- [x] 图书列表加载失败时展示错误提示

验收标准：

- [x] 页面首次加载可以展示后端返回的数据
- [x] 点击投票按钮后页面票数立即更新
- [x] 接口失败时用户能看到提示信息

## 8. 样式任务

- [x] 为页面添加基础布局
- [x] 为图书列表添加清晰的卡片或列表样式
- [x] 为票数添加醒目显示
- [x] 为投票按钮添加可点击状态
- [x] 为投票按钮添加禁用状态
- [x] 为错误提示添加明显样式
- [x] 适配基础移动端布局

验收标准：

- [x] 桌面端页面布局清晰
- [x] 移动端页面不出现明显错位
- [x] 按钮和票数容易识别

## 9. 错误处理任务

- [x] `book.json` 文件不存在时返回服务器错误
- [x] `book.json` 格式错误时返回服务器错误
- [x] 图书 ID 不存在时返回 404
- [x] 文件写入失败时返回服务器错误
- [x] 前端网络请求失败时展示错误提示
- [x] 前端接口返回失败时展示错误提示

验收标准：

- [x] 常见异常不会导致页面完全不可用
- [x] 常见异常不会导致后端进程直接崩溃
- [x] 用户能看到基础失败提示

## 10. 手动测试任务

- [x] 启动 Node.js 服务
- [x] 打开浏览器访问首页
- [x] 确认页面显示约 5 本图书
- [x] 点击任意一本书的投票按钮
- [x] 确认页面票数加 1
- [x] 刷新页面后确认票数保持不变
- [x] 检查 `data/book.json` 中对应票数已更新
- [x] 请求不存在的图书 ID，确认返回错误
- [x] 临时制造接口失败场景，确认前端有错误提示

## 11. 原始投票 MVP 完成标准

- [x] 用户无需登录即可投票（原始 MVP 已完成；新增登录模块实现后，将由“未登录用户不能投票”规则覆盖）
- [x] 页面可以展示约 5 本图书
- [x] 每本书都有当前票数
- [x] 每本书都有投票按钮
- [x] 点击投票后票数加 1
- [x] 投票结果写入 `data/book.json`
- [x] 刷新页面后票数不会丢失
- [x] 后端提供 `GET /api/books`
- [x] 后端提供 `POST /api/books/:id/vote`
- [x] 前端能处理基础失败情况

## 12. 用户注册和登录设计更新任务

- [x] 在 `spec.md` 中增加用户注册功能设计
- [x] 在 `spec.md` 中增加用户登录功能设计
- [x] 在 `spec.md` 中增加 `users.json` 数据结构设计
- [x] 在 `spec.md` 中增加认证相关 API 设计
- [x] 在 `spec.md` 中明确未登录用户不能投票
- [x] 在 `spec.md` 中明确登录后显示当前用户并支持退出登录

## 13. 用户数据文件任务

- [x] 创建 `data/users.json`
- [x] 将用户数据保存为数组结构
- [x] 每个用户包含 `id`、`username`、`passwordHash`、`createdAt`
- [x] 注册时保证 `username` 唯一
- [x] 密码不保存明文，保存密码哈希
- [x] 确保 `users.json` 是合法 JSON

验收标准：

- [x] 可以正常读取 `data/users.json`
- [x] 新注册用户可以写入 `data/users.json`
- [x] 重启服务后用户数据仍然保留

## 14. 后端认证模块任务

- [x] 实现读取 `data/users.json` 的逻辑
- [x] 实现写入 `data/users.json` 的逻辑
- [x] 实现密码哈希生成逻辑
- [x] 实现密码校验逻辑
- [x] 实现基础 token 生成逻辑
- [x] 实现根据 token 获取当前用户的逻辑
- [x] 注册、登录和投票接口复用统一错误响应格式

验收标准：

- [x] 后端可以创建新用户
- [x] 后端可以校验用户名和密码
- [x] 后端可以识别当前登录用户
- [x] 无效 token 不会被当作已登录状态

## 15. 认证 API 任务

### 用户注册接口

- [x] 实现 `POST /api/auth/register`
- [x] 校验用户名不能为空
- [x] 校验密码不能为空或过短
- [x] 校验用户名是否已存在
- [x] 注册成功后写入 `data/users.json`
- [x] 注册成功后返回用户基础信息，不返回密码或密码哈希
- [x] 用户名已存在时返回 409

验收标准：

- [x] 新用户名可以注册成功
- [x] 重复用户名不能注册
- [x] 注册响应不包含密码信息

### 用户登录接口

- [x] 实现 `POST /api/auth/login`
- [x] 校验用户名和密码
- [x] 登录成功后返回 token 和用户基础信息
- [x] 登录失败时返回 401
- [x] 登录响应不包含密码或密码哈希

验收标准：

- [x] 正确用户名和密码可以登录
- [x] 错误密码不能登录
- [x] 不存在的用户名不能登录

### 获取当前用户接口

- [x] 实现 `GET /api/auth/me`
- [x] 从 `Authorization` 请求头读取 token
- [x] token 有效时返回当前用户基础信息
- [x] token 缺失或无效时返回 401

验收标准：

- [x] 已登录用户可以获取当前用户信息
- [x] 未登录用户获取当前用户信息时返回错误

### 退出登录接口

- [x] 实现 `POST /api/auth/logout`
- [x] 若使用前端本地 token，后端可返回统一成功响应
- [x] 前端退出登录时清除本地 token

验收标准：

- [x] 用户可以退出登录
- [x] 退出登录后页面恢复未登录状态

## 16. 投票登录限制任务

- [x] 调整 `POST /api/books/:id/vote`，要求登录后才能投票
- [x] 投票接口读取并校验 `Authorization` 请求头
- [x] 未登录投票时返回 401
- [x] 登录凭证无效时返回 401
- [x] 已登录投票时保持原有票数加 1 逻辑
- [x] 不破坏 `GET /api/books` 公开访问能力

验收标准：

- [x] 未登录用户不能投票
- [x] 无效 token 不能投票
- [x] 已登录用户可以正常投票
- [x] 投票后仍然写回 `data/book.json`

## 17. 前端登录注册页面任务

- [x] 在页面中增加登录状态区域
- [x] 未登录时展示登录入口
- [x] 未登录时展示注册入口
- [x] 登录表单包含用户名和密码输入框
- [x] 注册表单包含用户名和密码输入框
- [x] 登录成功后显示当前用户名
- [x] 登录成功后隐藏登录和注册表单
- [x] 登录成功后展示退出登录按钮
- [x] 退出登录后清除本地 token
- [x] 退出登录后恢复未登录状态

验收标准：

- [x] 用户可以在页面完成注册
- [x] 用户可以在页面完成登录
- [x] 登录后能看到当前用户名
- [x] 用户可以退出登录

## 18. 前端投票状态任务

- [x] 页面加载时检查本地 token
- [x] 有 token 时请求 `GET /api/auth/me`
- [x] 根据登录状态更新投票按钮状态
- [x] 未登录点击投票时提示先登录
- [x] 已登录投票请求携带 `Authorization` 请求头
- [x] 登录过期或 token 无效时清除本地 token
- [x] 投票失败时展示后端返回的错误信息

验收标准：

- [x] 未登录用户点击投票会看到登录提示
- [x] 已登录用户点击投票可以成功更新票数
- [x] token 无效时页面恢复未登录状态

## 19. 认证错误处理任务

- [x] 注册用户名为空时展示错误提示
- [x] 注册密码为空或过短时展示错误提示
- [x] 注册用户名重复时展示错误提示
- [x] 登录用户名或密码错误时展示错误提示
- [x] 未登录投票时展示错误提示
- [x] 登录凭证无效时展示错误提示
- [x] `users.json` 读取失败时返回服务器错误
- [x] `users.json` 写入失败时返回服务器错误

验收标准：

- [x] 注册失败不会导致页面不可用
- [x] 登录失败不会导致页面不可用
- [x] 认证错误不会导致后端进程崩溃

## 20. 登录注册手动测试任务

- [ ] 启动 Node.js 服务
- [ ] 打开浏览器访问首页
- [ ] 未登录状态下点击投票，确认不能投票
- [ ] 注册一个新用户
- [ ] 使用新用户登录
- [ ] 登录后确认页面显示当前用户名
- [ ] 登录后点击投票，确认票数加 1
- [ ] 刷新页面后确认登录状态和票数符合预期
- [ ] 点击退出登录，确认恢复未登录状态
- [ ] 退出登录后再次点击投票，确认不能投票
- [ ] 重复注册同一用户名，确认返回错误
- [ ] 使用错误密码登录，确认返回错误

## 21. 登录注册功能完成标准

- [x] 用户数据使用 `data/users.json` 文件存储
- [x] 用户可以注册
- [x] 用户可以登录
- [x] 页面可以显示当前登录用户
- [x] 用户可以退出登录
- [x] 未登录用户不能投票
- [x] 已登录用户可以投票
- [x] 注册和登录失败时有错误提示
- [x] 现有图书列表展示功能不受影响
- [x] 现有票数持久化功能不受影响
